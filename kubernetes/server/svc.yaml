kind: Service
apiVersion: v1
metadata:
  name: go-natricon-service
  namespace: natricon-dev
spec:
  selector:
      app: go-natricon
  type: ClusterIP
  ports:
    - port: 5555
      targetPort: 5555